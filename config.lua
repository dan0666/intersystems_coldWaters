Config = {
    WinterWeatherTypes = { "SNOW", "XMAS", "BLIZZARD" },
    WaterDamageInterval = 3, -- Damage interval in seconds
    WaterDamageAmount = 1, -- Damage amount per interval
    NotificationMessage = "Ouchh, the water is so cold!",
    ShakeIntensity = 0.0, -- Shake effect
    BlurEffectDuration = 0, -- Blur duration in seconds
    SoundOn = true, -- Toggle to turn sounds on or off
    SoundVolume =  0.5
}
